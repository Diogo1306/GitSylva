Top-right result card (push/fetch/merge done). Drive `closing` from your controller to play the exit before unmount.

```jsx
<Notification title="Push concluído" sub="origin/main · 1 commit" onClose={dismiss} />
```