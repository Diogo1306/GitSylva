Segmented control for 2–3 mutually exclusive options (density, language, notification location).

```jsx
<Segmented value={lang} onChange={setLang} options={[{value:'pt',label:'Português'},{value:'en',label:'English'}]} />
```