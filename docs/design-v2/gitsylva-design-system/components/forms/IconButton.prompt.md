Square icon-only button for toolbars (terminal, settings, window controls).

```jsx
<IconButton title="Definições"><GearGlyph/></IconButton>
```

Pass a glyph/SVG as children. `active` gives it the selected background.